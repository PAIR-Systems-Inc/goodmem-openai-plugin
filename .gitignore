node_modules/
*.log
.DS_Store
*.tgz
.env

# SDK maintenance and regression scripts are part of the repository.
__pycache__/
bin/
obj/

# The gateway service lives in the private repo
# The hosted gateway is maintained separately; this repository holds only the
# public plugin bundles. A local working copy may remain here untracked.
gateway/

# editor workspace files — local to one machine, not part of the bundle
*.code-workspace
