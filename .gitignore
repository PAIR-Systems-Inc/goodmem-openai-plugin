node_modules/
*.log
.DS_Store
*.tgz
.env
__pycache__/

# Local assistant notes and generated submission files.
.agent-notes/
.submission-artifacts/
SUBMISSION_*.md

# internal tooling — never committed
scripts/

# Public SDK maintenance lives under tools/sdk-refs; builds use temporary directories.

# The hosted gateway is maintained separately; a local copy stays untracked.
gateway/

# editor workspace files — local to one machine, not part of the bundle
*.code-workspace
