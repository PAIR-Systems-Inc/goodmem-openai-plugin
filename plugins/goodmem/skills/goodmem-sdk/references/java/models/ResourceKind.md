<!-- sdk-ref package=ai.pairsys:goodmem-java registry=maven version=0.2.2 -->

# ResourceKind

- `INSTANCE` (`ResourceKind`):
- `USER` (`ResourceKind`):
- `SERVICE_IDENTITY` (`ResourceKind`):
- `SPACE` (`ResourceKind`):
- `API_KEY` (`ResourceKind`):
- `EMBEDDER` (`ResourceKind`):
- `RERANKER` (`ResourceKind`):
- `LLM` (`ResourceKind`):
- `MEMORY` (`ResourceKind`):
- `EXTENSION` (`ResourceKind`):
- `RETRIEVE_MEMORY_LOG_POLICY` (`ResourceKind`):

[Java](../../java.md)
