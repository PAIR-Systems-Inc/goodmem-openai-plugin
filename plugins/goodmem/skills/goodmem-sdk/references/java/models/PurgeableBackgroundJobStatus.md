<!-- sdk-ref package=ai.pairsys:goodmem-java registry=maven version=0.2.2 -->

# PurgeableBackgroundJobStatus

Terminal background job statuses eligible for purge requests.

- `BACKGROUND_JOB_SUCCEEDED` (`PurgeableBackgroundJobStatus`):
- `BACKGROUND_JOB_FAILED` (`PurgeableBackgroundJobStatus`):
- `BACKGROUND_JOB_CANCELED` (`PurgeableBackgroundJobStatus`):

[Java](../../java.md)
