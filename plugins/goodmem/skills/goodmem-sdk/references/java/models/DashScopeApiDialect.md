<!-- sdk-ref package=ai.pairsys:goodmem-java registry=maven version=0.2.2 -->

# DashScopeApiDialect

DashScope request and response API dialect

- `UNSPECIFIED` (`DashScopeApiDialect`):
- `EMBEDDING_NATIVE_TEXT` (`DashScopeApiDialect`):
- `EMBEDDING_NATIVE_CONTENTS` (`DashScopeApiDialect`):
- `LLM_NATIVE_TEXT` (`DashScopeApiDialect`):
- `LLM_NATIVE_MULTIMODAL` (`DashScopeApiDialect`):
- `RERANK_NATIVE_NESTED` (`DashScopeApiDialect`):
- `OPENAI_COMPATIBLE` (`DashScopeApiDialect`):
- `RERANK_COMPATIBLE_FLAT` (`DashScopeApiDialect`):

[Java](../../java.md)
