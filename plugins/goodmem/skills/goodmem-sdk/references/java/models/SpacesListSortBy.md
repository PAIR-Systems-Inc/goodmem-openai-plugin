<!-- sdk-ref package=ai.pairsys:goodmem-java registry=maven version=0.2.2 -->

# SpacesListSortBy

Typed wrapper for wire values declared via IR `enum=[\u2026]` on one or more record fields / query params. Unknown server values will fail Jackson deserialization \u2014 add them to the IR's `enum` array and regenerate.

- `CREATED_TIME` (`SpacesListSortBy`):
- `UPDATED_TIME` (`SpacesListSortBy`):
- `NAME` (`SpacesListSortBy`):

[Java](../../java.md)
