<!-- sdk-ref package=ai.pairsys:goodmem-java registry=maven version=0.2.2 -->

# BatchMemoryDeletionRequest

Request body for deleting memories using ID and/or filtered selectors

Prefer `builder()` to the canonical record constructor.
 Builder-based call sites remain source-compatible when optional fields are added in later SDK versions.

- `requests` (`java.util.List<BatchDeleteMemorySelectorRequest>`): Array of delete selectors

[Java](../../java.md)

Related types — open only those used by your request:

- [BatchDeleteMemorySelectorRequest](BatchDeleteMemorySelectorRequest.md)
