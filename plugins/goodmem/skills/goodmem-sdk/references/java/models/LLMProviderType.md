<!-- sdk-ref package=ai.pairsys:goodmem-java registry=maven version=0.2.2 -->

# LLMProviderType

LLM provider types

- `OPENAI` (`LLMProviderType`):
- `LITELLM_PROXY` (`LLMProviderType`):
- `OPEN_ROUTER` (`LLMProviderType`):
- `DASHSCOPE` (`LLMProviderType`):
- `VLLM` (`LLMProviderType`):
- `OLLAMA` (`LLMProviderType`):
- `LLAMA_CPP` (`LLMProviderType`):
- `CUSTOM_OPENAI_COMPATIBLE` (`LLMProviderType`):

[Java](../../java.md)
