<!-- sdk-ref package=goodmem registry=pypi version=0.1.34 -->

# BinaryContent

Binary content with MIME type for context items.

```python
from goodmem.models import BinaryContent
```

- `content_type` (`str`, required): MIME type of the binary content. JSON: `contentType`.
- `data` (`str`, required): Base64-encoded binary data.

[Python](../../python.md)
